<!-- toolkit-owned; edits are drift — see AGENTS.md -->

# Playbook: `toolkit`

Answer with this list, one plain line per verb:

- `catchup` — re-ground, tidy the record, report state and next action.
- `handoff` — save my place and commit the notes.
- `review` / `codereview <harness> <model> <effort> [<base>..<head>]` — hunt defects and verify fixes.
- `openreview <harness>` — judge the approach to a change or plan.
- `git` — delegated push, reconcile, remotes and branch cleanup.
- `new-project` — create a governed project.
- `update-governance` — apply the latest toolkit.
- `toolkit` — this list.

Mostly for working agents: `decision`, `plan`, `playbook`.
